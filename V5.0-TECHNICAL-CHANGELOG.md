# Dr. Beyaz Eşya Servisi — V5.0 Technical Cleanup

- Unified all HTML stylesheet references to `assets/style.css?v=5.0`.
- Corrected root-level English service pages that referenced `../assets` and `../index.html`.
- Consolidated nested `assets/assets/images` into `assets/images`.
- Replaced legacy `en-index.html` with a canonical redirect to `/en/`.
- Added URL query support to the error-code search page (`?q=`, `?brand=`, `?appliance=`).
- Added per-error-code WhatsApp service CTA to the error-code cards.
- Kept the existing V4.x design and content architecture; this pass is a compatibility/SEO foundation, not a visual redesign.
